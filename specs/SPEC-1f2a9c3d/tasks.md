- [ ] **T001** Agent: `/api/logs?tail=N` エンドポイントを実装（デフォ200、上限2000、空ファイルは200空ボディ）。  
- [ ] **T002** Agent: ログファイルパスを設定可能にし、単体テストで tail 切り出しを検証。  
- [ ] **T003** Coordinator: `/api/agents/:id/logs` プロキシを実装（timeout/非200→502）。  
- [ ] **T004** Contract test: 200/502 ケースをカバー（ノードスタブを用意）。  
- [ ] **T005** Dashboard: ログパネルが新APIでログを取得・表示するように差し替え（tail指定UI含む）。  
- [ ] **T006** E2E/Smoke: ダッシュボードからログが表示されることを手動またはスモークで確認。  
