name: Auto Merge PR

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
      - develop

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Enable Auto Merge
    runs-on: ubuntu-latest

    steps:
      - name: Enable auto-merge
        env:
          GH_TOKEN: ${{ secrets.PAT }}
        run: |
          if [ -z "${GH_TOKEN}" ]; then
            echo "::error::Required secret PAT is not configured. Set secrets.PAT (Personal Access Token with contents/pull-requests write) to enable auto-merge."
            exit 1
          fi

          if [ "${{ github.event.pull_request.draft }}" = "true" ]; then
            echo "⏭️ PR #${{ github.event.pull_request.number }} is draft. Auto-merge cannot be enabled yet."
            exit 0
          fi

          echo "🔁 Enabling auto-merge for PR #${{ github.event.pull_request.number }} targeting ${{ github.base_ref }}"
          gh pr merge --auto --merge ${{ github.event.pull_request.number }} --repo ${{ github.repository }}
