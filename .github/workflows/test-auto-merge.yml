# Test: Auto Merge Workflow
# 機能ID: SPEC-47c6f44c
# 目的: auto-merge.ymlの動作をテスト（TDD - RED）
# このテストは実装前なので失敗する必要がある

name: Test Auto Merge

on:
  workflow_dispatch:  # 手動実行専用（テスト用）
  push:
    branches: [test/**]

jobs:
  test-workflow-exists:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check if auto-merge.yml exists
        run: |
          if [ ! -f .github/workflows/auto-merge.yml ]; then
            echo "❌ ERROR: auto-merge.yml does not exist"
            echo "Expected: .github/workflows/auto-merge.yml"
            exit 1
          fi
          echo "✅ auto-merge.yml exists"

  test-workflow-structure:
    runs-on: ubuntu-latest
    needs: test-workflow-exists
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify workflow name
        run: |
          WORKFLOW_NAME=$(grep -A 1 "^name:" .github/workflows/auto-merge.yml | tail -1 | sed 's/name: //' | tr -d '"')
          if [ "$WORKFLOW_NAME" != "Auto Merge" ]; then
            echo "❌ ERROR: Workflow name is not 'Auto Merge'"
            echo "Expected: 'Auto Merge'"
            echo "Actual: '$WORKFLOW_NAME'"
            exit 1
          fi
          echo "✅ Workflow name is correct"

      - name: Verify workflow_run trigger
        run: |
          if ! grep -q "workflow_run:" .github/workflows/auto-merge.yml; then
            echo "❌ ERROR: workflow_run trigger not found"
            exit 1
          fi
          echo "✅ workflow_run trigger found"

          if ! grep -q 'workflows: \["Quality Checks"\]' .github/workflows/auto-merge.yml; then
            echo "❌ ERROR: 'Quality Checks' workflow not found in workflow_run trigger"
            exit 1
          fi
          echo "✅ Triggers on 'Quality Checks' workflow"

      - name: Verify required permissions
        run: |
          if ! grep -q "contents: write" .github/workflows/auto-merge.yml; then
            echo "❌ ERROR: 'contents: write' permission not found"
            exit 1
          fi
          echo "✅ 'contents: write' permission found"

          if ! grep -q "pull-requests: write" .github/workflows/auto-merge.yml; then
            echo "❌ ERROR: 'pull-requests: write' permission not found"
            exit 1
          fi
          echo "✅ 'pull-requests: write' permission found"

  test-expected-failure:
    runs-on: ubuntu-latest
    steps:
      - name: This test should fail (RED phase)
        run: |
          echo "🔴 RED Phase: This test is expected to FAIL"
          echo "Reason: auto-merge.yml has not been implemented yet"
          echo ""
          echo "Next step: Implement auto-merge.yml (T007) to make this test PASS (GREEN)"

          # Intentional failure for TDD RED phase
          exit 1
