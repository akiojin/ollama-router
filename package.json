{
  "name": "ollama-coordinator-release",
  "version": "0.0.0",
  "description": "Centralized management system for coordinating Ollama instances across multiple machines.",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:commits": "commitlint --from origin/main --to HEAD",
    "lint:md": "markdownlint '**/*.md' --ignore node_modules --ignore .git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^11.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^9.2.6",
    "@semantic-release/npm": "^11.0.2",
    "@semantic-release/release-notes-generator": "^12.1.0",
    "conventional-changelog-conventionalcommits": "^7.0.2",
    "husky": "^9.1.0",
    "markdownlint-cli": "^0.45.0",
    "semantic-release": "^23.0.0"
  }
}
