# Ollama Coordinator 環境変数設定

# === サーバー設定 ===
COORDINATOR_HOST=0.0.0.0
COORDINATOR_PORT=8080

# === ヘルスチェック設定 ===
HEALTH_CHECK_INTERVAL=30
AGENT_TIMEOUT=60

# === ロードバランサー設定 ===
LOAD_BALANCER_MODE=auto

# === データディレクトリ ===
OLLAMA_COORDINATOR_DATA_DIR=~/.ollama-coordinator

# === 認証設定 ===

# 認証機能の有効/無効（trueで無効化、プライベートネットワーク用）
AUTH_DISABLED=false

# JWT署名用シークレット（32文字以上のランダム文字列、未設定時は自動生成）
# 例: openssl rand -base64 32
# JWT_SECRET=your_random_secret_here

# 初回起動時の管理者アカウント作成（オプション、設定しない場合は対話式）
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your_secure_password_here
